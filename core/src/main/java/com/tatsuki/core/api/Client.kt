package com.tatsuki.core.api

import android.util.Log

class Client {

    companion object {
        private val TAG = Client::class.java.simpleName

        fun sample() {
            Log.d(TAG, "sample called.")
        }
    }
}